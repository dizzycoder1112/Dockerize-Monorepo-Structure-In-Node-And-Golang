PORT=8080
ENV=local

# DATABASE_URL is optional. When unset the server runs with in-memory
# repositories — clone-and-run with no infra. Set this to flip to the
# pgx-backed Postgres implementation.
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/layered_dev?sslmode=disable
